{
  "name": "akshare-node",
  "version": "0.1.0",
  "description": "Node wrapper for a Python AKShare bridge with SQLite cache",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}

