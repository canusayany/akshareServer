{
  "name": "akshare-node",
  "version": "0.1.0",
  "description": "Node wrapper for a Python AKShare bridge with SQLite cache",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "test": "node --test",
    "test:http": "node scripts/run_http_suite.js"
  },
  "engines": {
    "node": ">=20"
  }
}

